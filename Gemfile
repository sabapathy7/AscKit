# frozen_string_literal: true

# AscKit only needs fastlane at runtime. Every Ruby script under Scripts/ uses
# the stdlib only (openssl / net-http / json / base64) — no gems required.

source "https://rubygems.org"

gem "fastlane", "~> 2.225"
